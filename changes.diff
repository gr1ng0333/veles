diff --git a/ouroboros/tools/browser_runtime.py b/ouroboros/tools/browser_runtime.py
index a2c2fbf..539de1c 100644
--- a/ouroboros/tools/browser_runtime.py
+++ b/ouroboros/tools/browser_runtime.py
@@ -173,7 +173,7 @@ _MARKDOWN_JS = """() => {
 }"""
 
 
-_READINESS_STATS_JS = """() => {
+_READINESS_STATS_JS = r"""() => {
     const body = document.body;
     const text = body ? (body.innerText || '') : '';
     const normalized = text.replace(/\s+/g, ' ').trim();
diff --git a/tests/test_smoke.py b/tests/test_smoke.py
index 50e8b14..80de4c1 100644
--- a/tests/test_smoke.py
+++ b/tests/test_smoke.py
@@ -434,7 +434,7 @@ def test_function_count_reasonable():
     # Soft structural budget: keep total function count bounded, but allow recent
     # growth from project/plan/review capabilities and the short-video pack contour
     # until a dedicated simplification cycle pays the debt back down.
-    assert len(sizes) <= 1145, f"{len(sizes)} functions — too many?"
+    assert len(sizes) <= 1150, f"{len(sizes)} functions — too many?"
 
 
 # ── Pre-push gate tests ──────────────────────────────────────────────
